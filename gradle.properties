# Mod options
mod_id = sophisticatedbackpacks
mod_version = 3.20.2

# Dependencies
# adding a module also requires adding a dependency to the FMJ
port_lib_modules = common,extensions,fake_players,loot,model_loader,networking,transfer
